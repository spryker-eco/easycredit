{% extends model('component') %}

{% define config = {
    name: 'form'
} %}

{% define data = {
    form: required,
    legalText: required
} %}

{% block body %}
    {{ form_row(data.form.optInCheckbox, {
        rowAttr: {
            class: config.name ~ '__field ' ~ fieldLayoutClass
        },
        label: data.legalText
    }) }}
{% endblock %}
